-- Fase 0 baseline schemas
create schema if not exists core_pm;
create schema if not exists plm;
create schema if not exists spatial;
create schema if not exists finance;

-- Optional: set search path at session level in application code.

