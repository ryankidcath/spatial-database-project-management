/** Header bar seragam daftar tab Tabel / Obrolan / Aktivitas (tinggi = h-10 + py-2.5×2).
 *  Tanpa garis bawah — pemisah antar pane cukup dari border kolom, bukan garis header. */
export const WORKSPACE_TAB_LIST_HEADER_CLASS =
  "shrink-0 flex min-h-[3.75rem] w-full min-w-0 items-center px-4 py-2.5";

/** Label section di dalam rail (Tabel ruang kerja, Lapisan peta, dll.). */
export const WORKSPACE_RAIL_SECTION_LABEL_CLASS =
  "px-2 pb-1.5 pt-1 text-xs font-semibold uppercase tracking-wider text-muted-foreground";
