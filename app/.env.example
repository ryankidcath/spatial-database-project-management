# Salin ke .env.local di folder ini (npm run dev). Panduan deploy: ../DEPLOY.md
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Wajib untuk Preview/Production (redirect Auth). Lokal: http://localhost:3000
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# --- Pilot (F7-2), opsional ---
# Pendaftaran: kosong/open | closed | email_domain
# AUTH_SIGNUP_MODE=closed
# AUTH_SIGNUP_ALLOWED_EMAIL_DOMAIN=@contoh.go.id
# Banner di workspace: 1 / true + teks opsional
# NEXT_PUBLIC_SHOW_PILOT_BANNER=1
# NEXT_PUBLIC_PILOT_BANNER_TEXT=Versi pilot — hubungi admin untuk akses.
